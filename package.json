{
  "name": "citebridge",
  "version": "0.1.0",
  "private": true,
  "description": "Bridge Zotero references into Cursor LaTeX projects.",
  "type": "module",
  "scripts": {
    "build": "corepack pnpm -r build",
    "package": "corepack pnpm build && node scripts/package-cursor.mjs && node scripts/package-zotero.mjs",
    "package:cursor": "node scripts/package-cursor.mjs",
    "package:zotero": "node scripts/package-zotero.mjs",
    "check": "corepack pnpm -r check"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@9.7.0"
}
